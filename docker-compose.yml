version: '3.8'

services:
  na-lovu:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./uploads:/app/uploads
      - ./questions:/app/questions
    environment:
      - PORT=3000
    restart: unless-stopped
